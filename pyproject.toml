[project]
name = "from-scratch"
version = "0.1.0"
description = "Transformer building blocks from scratch."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.1",
    "sentencepiece>=0.2.1",
    "torch>=2.13.0",
]
